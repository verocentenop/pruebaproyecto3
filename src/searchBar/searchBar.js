import './searchBar.css'

export const searchBar = () => {
  const searchBarElements = `<div class = "searchBar-container"> <input type = 'search' class = "search-bar" placeholder= "Search..." id = "input"> </div>`

  return searchBarElements
}
